import styled from 'styled-components';

export default styled.h1`
  font-family: ${({ theme }) => theme.fonts.quicksand};
  font-weight: normal;
  text-align: center;
  margin-bottom: 0;
  margin-top: 10px;
  flex: 1;
`;
